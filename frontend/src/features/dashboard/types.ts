/**
 * Contrato do Dashboard vem de `@beautyflow/shared-types` (GET /dashboard do backend
 * NestJS) — sem duplicar aqui, mesmo padrão dos demais módulos.
 */
export type { DashboardProximoAtendimento, DashboardResponse, DashboardResumo } from "@beautyflow/shared-types";
