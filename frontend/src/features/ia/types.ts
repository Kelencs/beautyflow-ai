/**
 * Contrato de IA vem de `@beautyflow/shared-types` (GET /ia do backend NestJS) — sem
 * duplicar aqui, mesmo padrão já usado nos demais módulos.
 */
export type {
  IaCapacidade,
  IaComportamento,
  IaConfiguracao,
  IaIntencao,
  IaIntencaoCodigo,
  IaInteracao,
  IaMemoria,
  IaMemoriaCliente,
  IaResumo,
  IaStatus,
  StatusProcessamentoIa,
} from "@beautyflow/shared-types";
